package flat/compiler

import flat/io/File
import flat/lexer/Lexer

class {
  public static async main(String[] args) {
    let stream = Lexer().lex(File(args[1]))

    stream.on("data", { Console.log(_) })

    stream.waitFor("close")

    Console.log("Done!")
  }
}
